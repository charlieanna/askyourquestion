<%= form_for :subscriber,url: user_join_path(current_user) do |f|%>
  <%= f.label :code%>
  <%= f.text_field :code ,placeholder: "Enter the code of the event to join:",required: true%>
  <%= f.submit "Join Event",class:"btn btn-primary"%>
<% end %>